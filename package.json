{
  "name": "curry-di",
  "version": "0.9.1",
  "description": "Similar to the standard curry function found in functional programming but allowing parameters to applied non-sequentially based on names matching a supplied object",
  "author": "Matthew Forrester <curry-di@keyboardwritescode.com>",
  "scripts": {
    "test": "mocha tests"
  },
  "dependencies": {
    "get-parameter-names": "~0.1.1"
  },
  "devDependencies": {
    "mocha": "~1.12.1",
    "sinon": "~1.7.3",
    "expect.js": "~0.2.0"
  },
  "repository": {
    "url": "https://github.com/forbesmyester/curry-di.git"
  }
}
